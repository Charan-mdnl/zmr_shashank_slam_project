# SLAM Toolbox Parameters — Tuned for Gazebo Simulation
#
# DIFFERENCES FROM REAL ROBOT CONFIG:
# - use_sim_time: true  (Gazebo runs on simulated clock, not wall clock)
# - Less noise compensation needed (simulated LiDAR is cleaner)
# - Can afford higher resolution since there's no real-time constraint

slam_toolbox:
  ros__parameters:
    # ── Solver ──
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # ── Frame Names ──
    # These MUST match what Gazebo's diff_drive plugin publishes
    odom_frame: odom
    map_frame: map
    base_frame: base_footprint
    scan_topic: /scan

    # CRITICAL: use simulated time from Gazebo, not real wall clock
    use_sim_time: true

    # ── Mapping Mode ──
    use_map_saver: true
    mode: mapping

    # ── Scan Processing ──
    use_scan_matching: true
    use_scan_barycenter: true
    minimum_travel_distance: 0.0
    minimum_travel_heading: 0.0
    scan_buffer_size: 10
    scan_buffer_maximum_scan_distance: 25.0
    link_match_minimum_response_fine: 0.1
    link_scan_maximum_distance: 3.0

    # ── Loop Closure ──
    # When the robot returns to a place it's been before, loop closure
    # "corrects" the accumulated drift in the map
    loop_search_maximum_distance: 5.0
    do_loop_closing: true
    loop_match_minimum_chain_size: 10
    loop_match_maximum_variance_coarse: 3.0
    loop_match_minimum_response_coarse: 0.35
    loop_match_minimum_response_fine: 0.45

    # ── Correlation Search ──
    correlation_search_space_dimension: 0.5
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.1

    # ── Loop Closure Search ──
    loop_search_space_dimension: 8.0
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03

    # ── Scan Matcher Tuning ──
    distance_variance_penalty: 0.5
    angle_variance_penalty: 1.0
    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.349
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true

    # ── Map Quality ──
    resolution: 0.05          # 5cm per grid cell
    max_laser_range: 25.0     # Match A3M1 range
    minimum_time_interval: 0.1
    transform_timeout: 0.5
    tf_buffer_duration: 30.0
    stack_size_to_use: 40000000

    enable_interactive_mode: true
